services:
  - type: web
    name: forecasting-workforce-mock-mcp
    runtime: node
    plan: free
    rootDir: mock-forecasting-mcp
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
